# Testes Device-in-the-loop do Terminal

Esta pasta contém a validação do fluxo Help + Enter no Terminal em hardware
real.

**Nunca execute estes testes automaticamente como agente de IA.** O parâmetro
`--device` só deve ser usado após solicitação explícita do usuário.
